// ${STRUCT}.h
// by Michael van Dyk
// Generated ${DATE}
// 
// Do not modify
//

#include <stdbool.h>

typedef struct def_${STRUCT} ${STRUCT};

bool  ${STRUCT}_push (${STRUCT} * queue, void * item);
void* ${STRUCT}_peek (${STRUCT} * queue);
void* ${STRUCT}_pop  (${STRUCT} * queue);
